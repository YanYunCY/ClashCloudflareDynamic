<!--
提交前请先脱敏：不要在改动、截图或日志中包含真实的 UUID、密码、控制器 secret、
SNI 域名、WebSocket 路径、provider 文件或节点账号。占位一律用 example.com、<你的-uuid> 之类。
-->

## 变更说明 (Summary)

<!-- 这个 PR 做了什么？为什么需要？关联的 issue 请用 #编号 引用。 -->

## 测试情况 (Testing)

<!-- 勾选已在本地通过的检查（参见 CONTRIBUTING.md） -->

- [ ] Python 测试通过（`python -m unittest discover -s tests/python -p "test_*.py"`）
- [ ] PowerShell 隔离测试通过（`tests/windows/*.ps1`）
- [ ] 隐私检查通过（`python tools/privacy_check.py`）

## CHANGELOG (Changelog)

- [ ] 已更新 `CHANGELOG.md`
- [ ] 本次改动无需记录（例如仅文档或注释）

## 补充信息 (Additional context)

<!-- 截图、兼容性说明或其他评审需要了解的信息（同样先脱敏） -->
